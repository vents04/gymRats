const { StyleSheet } = require('react-native');

module.exports = StyleSheet.create({
    chatsTopbarContainer: {
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center',
    },
    chatsTopbarContainerTitle: {
        fontFamily: "SpartanBlack",
        fontSize: 30
    }
});